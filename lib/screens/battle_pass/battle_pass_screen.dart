import 'package:flutter/material.dart';

class BattlePassScreen extends StatelessWidget {
  const BattlePassScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Battle Pass - Coming Soon'),
    );
  }
}